import {StyleSheet} from 'react-native';

export const styledAllPoints = StyleSheet.create({
  actions: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  button: {
    width: '48%',
  },
});
